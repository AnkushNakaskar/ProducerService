spring:
  application:
    name: producerService
      
---
spring:
  profiles: test
  cloud:
    consul:
      enabled: false    
---

spring:
  profiles: default
  cloud:
    consul:
        enabled: false

---
spring:
  profiles: qa
  cloud:
    consul:
      enabled: false